> Execution route updated: the user chose local Codex. Read HANDOFF_LOCAL_JA.md for the current continuation instructions; the Actions route below is a historical proposal, not approval.

# Continuation checkpoint — 2026-09-07

## User instruction

Reproduce arXiv:2608.23110 in Julia. Establish agreement for the 2D rising
bubble before solving Section 4.4. Upload reproduced code and a README linking
the paper to `Makoto523sys/weakly-compressible-subcycling-reproduction`.

## What exists

- Initial Julia implementation of the closed-box bubble solvers, tests, runners,
  metrics, VTK outputs and a quantitative velocity-comparison script.
- External reference curves extracted from Figure 5 SVG, with provenance hashes.
- Porous case specification including visually read dimensions and shifts.
- An unexecuted GitHub Actions workflow for tests, coarse runs and optional full
  bubble runs. Full jobs have a 180-minute cap; runtime sufficiency is unknown.
- README and verification plan explicitly mark the absence of execution.

## Actual verification performed

- Read Sections 2–3, 4.2, 4.4 and Appendix A; inspected Figures 5 and 10.
- Checked repository metadata through the GitHub plugin: public, default `main`,
  reported size 0, push permission. Root AGENTS.md request returned 404.
- Searched the local runtime locations and Python bindings: no Julia runtime.
- Checked TOML/YAML syntax and reference CSV monotonicity using Python.
- Recomputed capillary and viscous limits independently in Python:
  8.11325422284e-6 s and 2.54313151042e-5 s for the paper bubble grid.
- Source reviewed for discretization/units; this is not a Julia syntax or execution test.

No Julia tests, bubble trajectories, porous simulations or GitHub writes occurred.
The external runtime connectivity request failed with
`network approval was cancelled before a decision was returned`.
Do not describe that as successful installation or as a scientific solver failure.

## Next action

Resolve execution access. A concrete alternative is to publish this explicitly
unvalidated code on `validation/julia` and run the prepared Actions workflow.
That changes the user's requested validation-before-upload order, so confirm
that alternate order first. The repository was reported empty: inspect refs
before choosing how to bootstrap it; do not overwrite any subsequently added work.
Do not assume workflow_dispatch works until the workflow is available on the
repository's default branch. Branch push triggers are available for initial
unit/smoke checks.

Then run the tests, fix any defects, add static two-phase force-balance checks,
and measure cost on a modest grid before running 256×512 through 0.07 s. Record
all failures. Review the documented numerical differences before claiming agreement.
Only after bubble acceptance should cut-cell/contact-angle work begin.

Do not confuse `reference/*.csv` with results generated by this Julia solver.
Do not claim H100-equivalent speedups from this CPU/PCG implementation.
